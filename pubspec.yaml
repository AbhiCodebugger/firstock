name: mindorigin
description: Mini Investment Portfolio Dashboard with Live WebSocket Data
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # Navigation

  # Networking
  internet_connection_checker_plus: ^2.9.1+2
  dio: ^5.9.2

  # State Management
  fpdart: ^1.2.0
  equatable: ^2.0.7
  flutter_bloc: ^9.1.1

  # Backend – Firebase

  # Backend – Supabase

  # Backend – Appwrite

  # Storage
  shared_preferences: ^2.5.4

  # Shimmer / Skeleton
  skeletonizer: ^2.1.3

  # Responsive UI
  flutter_screenutil: ^5.9.3

  # Animations
  flutter_animate: ^4.5.2

  # Environment
  flutter_dotenv: ^6.0.0

  # Media

  # Utilities

  # Device & System


  # Localization
  easy_localization: ^3.0.8

  flutter_native_splash: ^2.4.7

  # Charts
  fl_chart: ^1.1.1

  # Live feed
  web_socket_channel: ^3.0.3

  # Typography
  google_fonts: ^6.3.2
  firebase_core: ^4.14.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  bloc_test: ^10.0.0
  mocktail: ^1.0.4

  # Code Generation

dependency_overrides:

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/translations/
    - .env.dev
    - .env.qa
    - .env.prod

